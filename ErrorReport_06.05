1. /src/core/model/version.cc include path problem
#include "ns3/version-defines.h"
but there is no such a header file at all, under the same folder there is a program mit a suffix ".h.in", what does that mean?
Solution: File end with .h.in means a corrresponding header file will be generated by configure phase. 


2. /src/core/examples/build-version-example.cc 
#include "ns3/version.h"
inlude path problem
Solution: this error occurs because the author forgot to add path of version.h to include(ns3) folder.  

3. in fortiss version ns3-3.34, in directory src/doc/deprecated-example.h:

